{
  "name": "cra-template-mk",
  "version": "0.0.1",
  "devDependencies": {
    "np": "^7.6.3"
  },
  "scripts": {
    "release": "np",
    "test": "echo \"Error: no test specified\" && exit 0"
  }
}
